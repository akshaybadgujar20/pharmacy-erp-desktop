---
description: Medicine module conventions — read memory doc before editing backend/src/medicine
globs: backend/src/medicine/**
alwaysApply: false
---

# Medicine module

**Read the full memory model first:** [`.cursor/rules/docs/medicine-module.md`](docs/medicine-module.md)

Org-global medicine master data. Mirrors party/security CRUD patterns.

## Golden rules

1. **Reads** use `prisma.client`; **writes** use `unitOfWork.run` only.
2. **All mutations** — audit + outbox in the same transaction (`AuditModule.MEDICINE`).
3. **Org-global** — no branch scoping on list/get.
4. **Manufacturer** mirrors Supplier: `partyId` in create body, one per party, immutable `partyId`.
5. **MedicineSalt** nested under `/medicines/:medicineId/salts`; hard delete junction rows; `PUT .../replace` bulk replace.
6. **Medicine** `medicineCode` immutable after create; `medicineName` unique per `manufacturerId`.
7. **Category** flat CRUD with `assertNoCategoryCycle` on parent changes.
8. **System rows** (`isSystemSchedule`, `isSystemUnit`): full CRUD; delete blocked only by FK refs.

## Delete guards

| Entity | Block when |
|--------|------------|
| Medicine | batches, stock movements, purchase/sales line items, price list items |
| Manufacturer | medicines reference |
| Category | child categories or medicines |
| Generic | salt compositions |
| Schedule | medicines |
| UOM | medicines, salt compositions, transaction line items |
| SaltComposition | medicine salts |

## Layer checklist

- **DTOs:** update requires `version`; nested MedicineSalt create has no `medicineId` in body.
- **Mappers:** one-way `to{Entity}Response()`; IDs as strings; decimals as strings.
- **Permissions:** `MASTER:RESOURCE:ACTION` on every endpoint.
- **Constants:** [`medicine.constants.ts`](../backend/src/medicine/constants/medicine.constants.ts).
- **Utils:** [`medicine.util.ts`](../backend/src/medicine/utils/medicine.util.ts).

## Not implemented

Unit/persistence/e2e tests, Angular clients, category tree endpoint, medicine code auto-sequence.

## Architecture reference

Table specs: [`medicine_master.md`](../docs/pharmacy_erp_architecture_docs/database/tables/medicine_master/medicine_master.md). Domain: [`product.md`](../docs/pharmacy_erp_architecture_docs/domain/product.md).
