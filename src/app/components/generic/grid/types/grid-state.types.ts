export interface GridStateConfig {
  persist?: boolean;
  storageKey?: string;
  columns?: boolean;
  sorting?: boolean;
  filtering?: boolean;
  pageSize?: boolean;
  columnWidths?: boolean;
  columnOrder?: boolean;
}
