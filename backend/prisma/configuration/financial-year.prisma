model FinancialYear {
  id BigInt @id @default(autoincrement())

  uuid String @unique @default(uuid())

  companyId BigInt  @map("company_id")
  branchId  BigInt? @map("branch_id")

  financialYearCode String @map("financial_year_code")
  financialYearName String @map("financial_year_name")

  startDate BigInt @map("start_date")
  endDate   BigInt @map("end_date")

  status String

  isCurrent Boolean @default(false) @map("is_current")

  closingDate BigInt? @map("closing_date")

  remarks String?

  createdAt BigInt  @map("created_at")
  updatedAt BigInt  @map("updated_at")
  deletedAt BigInt? @map("deleted_at")

  version BigInt @default(1)

  company Company @relation(fields: [companyId], references: [id])
  branch  Branch? @relation(fields: [branchId], references: [id])

  @@unique([companyId, financialYearCode])
  @@index([companyId])
  @@index([branchId])
  @@index([status])
  @@index([isCurrent])
}
