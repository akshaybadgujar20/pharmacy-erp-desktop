model Batch {
  id BigInt @id @default(autoincrement())

  uuid String @unique @default(uuid())

  medicineId BigInt @map("medicine_id")

  batchNumber String @map("batch_number")

  manufacturingDate BigInt? @map("manufacturing_date")
  expiryDate        BigInt  @map("expiry_date")

  purchaseRate Decimal @map("purchase_rate") // Lot cost snapshot at receipt
  mrp          Decimal // Statutory MRP printed on pack/lot

  barcode String?

  isActive Boolean @default(true) @map("is_active")

  createdAt BigInt  @map("created_at")
  updatedAt BigInt  @map("updated_at")
  deletedAt BigInt? @map("deleted_at")
  updatedBy BigInt? @map("updated_by")
  deletedBy BigInt? @map("deleted_by")

  version BigInt @default(1)

  medicine      Medicine @relation(fields: [medicineId], references: [id])
  updatedByUser User?    @relation("BatchUpdatedBy", fields: [updatedBy], references: [id])
  deletedByUser User?    @relation("BatchDeletedBy", fields: [deletedBy], references: [id])

  stocks               Stock[]
  stockMovements       StockMovement[]
  stockAdjustmentItems StockAdjustmentItem[]
  stockTransferItems   StockTransferItem[]
  stockTakeItems       StockTakeItem[]

  purchaseInvoiceItems PurchaseInvoiceItem[]
  purchaseReturnItems  PurchaseReturnItem[]
  salesInvoiceItems    SalesInvoiceItem[]
  salesReturnItems     SalesReturnItem[]
  goodsReceiptItems    GoodsReceiptItem[]

  @@unique([medicineId, batchNumber])
  @@index([medicineId])
  @@index([medicineId, expiryDate])
  @@index([expiryDate])
  @@index([barcode])
  @@index([isActive])
}
